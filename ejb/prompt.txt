create a java model based on this table : { Table }... here is an example that you need to follow : { Model } ... if there is a FK in the table, don't just add the ID of the FK as an attributes, but add directly the Model Class as an attribute  ... 